# Contacting Audit

## Packages Reviewed
- `packages/contacting` (`aiarmada/contacting`) — core domain: `src/Models` (3: `ContactMethod`, `SocialProfile`, `ContactSnapshot` — all `HasOwner` + `HasOwnerScopeConfig`, key `contacting.features.owner`), `src/Actions` (10: `Create/Update/Normalize/SetPrimary` ×2 channels + `CreateContactSnapshotAction` + `BuildContactLinksAction`), `src/Concerns` (`HasContactMethods`, `HasSocialProfiles`), `src/Contracts` (2 normalizers), `src/Data` (4 Spatie Data with named constructors), `src/Enums` (`ContactMethodType`, `ContactPurpose`, `SocialPlatform`), `src/Support` (6: `ContactingModelReferenceGuard`, `SocialProfileConfig`, 4 normalizers), `config/contacting.php`, `database/migrations/2000_01_01_000001..000003`, `database/factories` (3), `src/ContactingServiceProvider.php`, `CONTEXT.md`, `docs/01..04 + 99`. No `routes/`, no `tests/`.
- `packages/filament-contacting` (`aiarmada/filament-contacting`) — Filament adapter: `src/Resources` (3: `ContactMethodResource`, `SocialProfileResource`, `ContactSnapshotResource` + Pages), `src/RelationManagers` (2), `src/Schemas` (5) + `src/Tables` (3), `src/Imports` (2) + `src/Exports` (2), `src/Support` (`GuardsContactingUi`, `ContactingFilamentConfig`, `ResolvesContactingModels`), `config/filament-contacting.php`, `src/FilamentContactingServiceProvider.php`, `src/FilamentContactingPlugin.php`, `CONTEXT.md`, `docs`. No `routes/`, no `tests/`.

## Overall Assessment
- Quality: Strongest core in this batch and the reference implementation for owner-scoped polymorphic attach: `HasOwner`+`HasOwnerScopeConfig` on all 3 models, `OwnerWriteGuard`-backed `ContactingModelReferenceGuard` on every write path, `OwnerContext::withOwner` in snapshot persistence, `OwnerUiScope` in all 3 Filament resources, `HasContactMethods/HasSocialProfiles` cascade deletes, per-channel normalize actions, `SocialProfileConfig` prefix/suffix URL builder, `ContactMethodData::email/phone/website` named constructors. Real adoption proves the design (`customers.Customer` + `customers.Address`, `affiliates.Affiliate`, `events.Venue`/`EventOrganizer`, `feedback` models all use the traits).
- Health risks (ranked): (1) **Dual contact storage with no canonical rule** — adopters keep native `email`/`phone`/`website`/`contact_email` COLUMNS alongside contacting rows (`customers.Customer.email/phone`, `customers.Address.phone`, `events.EventOrganizer.email/phone`, `events.Venue` computed accessors, `affiliates.Affiliate.contact_email/website/phone` + pending-write accessors): every read must decide column-vs-row, every write must sync both, and `Customer.getFormattedAddress/toShippingLabel` already encode divergent fallback logic; (2) **Primary-flag races + asymmetric implementations** — `ContactMethod::syncSiblingPrimaryFlags` locks parent + siblings transactionally while `SocialProfile::syncSiblingPrimaryFlags` is a bare mass-update with no lock/transaction and no owner scoping on the demote query; neither has a DB backstop; (3) **`order_column`/`sort_order` alias hack** — both models expose `order_column` get/set aliases over the real `sort_order` column (migration has only `sort_order`), suggesting a half-finished rename that doubles the API and risks Filament table-sorting on a non-column; (4) **Snapshot semantics half-built** — `CreateContactSnapshotAction::persistSnapshot` returns an UNSAVED model when `features.contact_snapshots=false` (silent no-persist), `fromBundle` loops N+M saves with no transaction, snapshots carry `owner` copied from source but `snapshotable` owner unchecked; (5) **Inconsistent timestamps** — contacting migrations use `$table->timestamps()` (tz-naive) while every other pair in this batch uses `timestampsTz()`, and lifecycle columns are correctly `timestampTz` — mixed precision in one table; (6) zero tests despite being the most-depended-upon package in the batch.
- Refactor size: Small-Medium, code-only. Unify primary-flag sync, remove/finish the alias hack, make snapshot persistence explicit, document the column-vs-row canonical rule, align timestamps on next table touch, add Pest suites. Estimated ~12 files touched, 1 new file max (`Support/ContactPrimaryGuard` or shared trait), no structural migration strictly required.

## Migration Impact
**Migration Required: NO**
- Tables: `contact_methods`, `contact_social_profiles` (name is `social_profiles` via config — migration filename says `contact_social_profiles` but `getTable()` resolves `contacting.database.tables.social_profiles`; confirm actual table name in a fresh migrate — filename/table mismatch is docs-only), `contact_snapshots` (all `config('contacting.database.tables.*')`). No renames, no adds, no drops recommended in this audit.
- Columns: no changes. `owner_type/owner_id` (`nullableUuidMorphs`), `contactable/socialable/snapshotable` morphs, `type/purpose/platform/channel`, `value/normalized_value/display_value/url`, `is_primary/is_public/is_verified`, `verified_at/valid_from/valid_until` (`timestampTz`), `sort_order`, `metadata/payload` (configurable `json` via `commerce_json_column_type`) — all correctly shaped. The `order_column` alias needs NO column (it overlays `sort_order`; fix is code deletion, not schema).
- Indexes/constraints: no `constrained()`/`cascadeOnDelete()` found — compliant. Recommended NON-required future hardenings (explicitly out of scope, online index-only if ever adopted): partial unique `(contactable_type, contactable_id, type, purpose) WHERE is_primary` and the social equivalent to back the primary invariant; `index(['contactable_type','contactable_id','is_primary'])` to speed demote queries. None required for correctness of this audit's fixes (code guards suffice at contacting scale).
- Timestamps note: `created_at/updated_at` are tz-naive `timestamps()` while lifecycle columns are `timestampTz` — normalizing to `timestampsTz()` would be a migration; explicitly NOT recommended now (mixed precision is harmless; change only when the tables are next rebuilt for another reason, with a timezone-backfill check).
- Data migration: none.

## Package Responsibilities
- `contacting` owns: polymorphic contact methods (`ContactMethod` + `HasContactMethods` + create/update/normalize/set-primary), social profiles (`SocialProfile` + `HasSocialProfiles` + same), contact snapshots (`ContactSnapshot` + `CreateContactSnapshotAction`), channel normalization (email/phone/URL/handle normalizers + `SocialProfileConfig` URL builder), link building (`BuildContactLinksAction`), reference guard (`ContactingModelReferenceGuard`).
- `contacting` does NOT own: message sending history (belongs to `communications`), customer/affiliate/event domain logic (consumers), address geography (belongs to `addressing`).
- `filament-contacting` should own only: relation-managers-first UI (default per config: `standalone_resources=false`, `relation_managers=true`), 3 resources (off by default) + schemas/tables, imports/exports, guards/config support. It currently does — the thinnest adapter in the batch, correctly delegating normalization/validation to core.

## Deep-Dive Findings, Second Pass (2026-09-07 re-audit)

## Re-check Notes (2026-09-07, verified against source)
- Architecture High (column/row duality): CONFIRMED — `Customer.email/phone` columns + `HasContactMethods/HasSocialProfiles` on the same model; `EventOrganizer.email/phone` + traits with computed `resolvePrimaryContactValue` accessors; `rg HasContactMethods|HasSocialProfiles packages/customers/src packages/events/src packages/affiliates/src` shows trait use alongside native columns in all three consumers; no `syncPrimaryColumns` helper exists.
- Architecture High (asymmetric primary sync): CONFIRMED — `ContactMethod::syncSiblingPrimaryFlags()` (lines 237–255: `DB::transaction` + parent `lockForUpdate` + scoped demote with `lockForUpdate`, NULL-aware branches) vs `SocialProfile::syncSiblingPrimaryFlags()` (lines 247–258: bare `SocialProfile::query()->where(...)->update(...)` — no transaction, no lock, no owner scope, `where(col, null)` NULL semantics); neither has a `wasChanged('is_primary')` guard (both run on every `saved`); no partial-unique backstop.
- Architecture Medium (`order_column` alias): CONFIRMED — both models list `'order_column'` + `'sort_order'` in `$fillable` with `setAttribute`/`getAttribute` interceptors; migrations carry only `sort_order`. Blast radius outside contacting: `rg order_column packages --glob '!packages/contacting/**'` hits ONLY `feedback` migrations/actions (own real column, unrelated) — deletion is safe in-repo.
- Architecture Medium (snapshot phantom + non-atomic bundle + owner mismatch): CONFIRMED — `persistSnapshot()` returns the unsaved `$snapshot` when `contacting.features.contact_snapshots=false`; `fromBundle()` loops N+M `fromContactMethod/fromSocialProfile` saves with no `DB::transaction`; `makeSnapshot()` copies `owner` from the SOURCE row and never compares it to the `snapshotable` owner. Note the finding's cited line numbers (122–133) drifted — the logic now lives in `persistSnapshot()` + `makeSnapshot()`/`fromBundle()` as cited here.
- Code Quality (`SocialPlatform` enum reads config): CONFIRMED — `SocialPlatform::label()` reads `config('contacting.social_profiles.platforms.*.label')` at runtime (enum depends on config); `strict_social_platforms/strict_contact_types` both default `false` in config.
- Code Quality (`BuildContactLinksAction` unused): CONFIRMED — `rg BuildContactLinks packages --glob '!packages/contacting/**'` returns zero hits; only `contacting/docs` + `CONTEXT.md` reference it.
- Filament Medium (importer/exporter owner handling): CONFIRMED UNVERIFIED-AS-FLAGGED — `rg Owner|owner|withOwner packages/filament-contacting/src/Imports/ContactMethodImporter.php` returns zero hits (importer resolves no owner per-row today); exporter scoping likewise unconfirmed. The finding stays a verify-and-harden item, not a failed assertion.
- Security High (guard-before-demote ordering): CONFIRMED — guards run in `saving` (`normalizeForSave → guardAllowedType/Platform → guardContactableOwner/guardSocialableOwner`), demote runs on `saved` (post-save), so the ordering is safe TODAY but coupled implicitly; demote queries carry no owner scope (same root as the Architecture High, write side).
- Testing High (zero tests): CONFIRMED — `ls packages/contacting/tests packages/filament-contacting/tests` both missing, despite 3 factories shipping.
- Severity normalization: no Critical in this pair (owner scoping is present and correct; the Highs are significant arch/behavior defects, not breach-class). The `is_public`-default tightening is Medium with an explicit YES breaking change (privacy tightening, intended).

## Architecture Findings
- Severity: High | Location: `packages/customers/src/Models/Customer.php` (`email`, `phone` columns + `HasContactMethods/HasSocialProfiles`), `packages/customers/src/Models/Address.php` (`phone` column + `HasContactMethods`), `packages/events/src/Models/EventOrganizer.php` (`email/phone` + traits), `packages/affiliates/src/Models/Affiliate.php` (`contact_email/website_url` + pending accessors + `addContactMethod` in `saving` hooks), `packages/contacting/src/Concerns/HasContactMethods.php::resolveEmail/resolvePhone` | Problem: Every adopter duplicates the contact value in a native column AND in contacting rows with package-local sync rules: `Customer` writes columns directly (no sync to rows on `creating`); `Affiliate` syncs pending→row in model hooks (`$pendingContactEmail` pattern, lines ~480–546); `customers.Address::getFormattedAddress/toShippingLabel` prefer row-over-column when relation loaded, column otherwise (render-dependent output). `HasContactMethods::resolveEmail()` reads ROWS while most callers (`Order::billingAddress->email`, checkout) read COLUMNS. There is no documented canonical source, no `syncColumnsToRows` action, and `contacting` provides no bridge. Verified repo-wide: `rg contact_email|->phone|->email packages/customers/src packages/events/src packages/affiliates/src` hits columns everywhere alongside trait usage. | Why It Matters: Same email stored twice, diverging (column updated, row stale); identity resolution (`CustomerResolver` email matching) and communications (which read rows) disagree; GDPR deletion must clear both. | Recommended Fix: Declare the rule in code+docs (recommended: contacting ROWS are canonical for multi-value/history; native columns are write-through caches for the primary value, synced by a single `SyncPrimaryContactColumns` concern or action invoked from `ContactMethod::saved/deleted` via model events on the owning side). Short-term (this audit): add `HasContactMethods::syncPrimaryColumns(['email','phone'])` opt-in helper + document per-consumer mapping (`Customer.email ↔ primary email row`, `Address.phone ↔ primary phone row`); long-term: new models MUST NOT add native email/phone columns (lint in review). Update `contacting/docs/04-usage.md` + each consumer's docs same pass. | Breaking Change: NO (additive helper + docs; no sync enforced until consumers opt in) | Affected Packages: `contacting`, `customers`, `events`, `affiliates`, `feedback`, `communications`, `orders`, `checkout` | Required Dependent Changes: Each consumer adopts the helper and backfills columns from primary rows once (one-off artisan, not a migration). | Migration Required: NO
- Severity: Medium | Location: `packages/contacting/src/Models/ContactMethod.php:78-98`, `packages/contacting/src/Models/SocialProfile.php:80-100` (`order_column` get/set alias over `sort_order`), `database/migrations/*` (only `sort_order` column), `packages/filament-contacting/src/Tables/*` (verify sort column referenced) | Problem: Half-finished column rename left as runtime alias: `$fillable` lists BOTH `order_column` and `sort_order`; `setAttribute/getAttribute` intercept `order_column` → `sort_order`. Consequences: `orderBy('order_column')` generates SQL on a non-column (error); `toArray()` emits `sort_order` only (alias invisible); Filament table sorting on `order_column` breaks; Spatie-Data round-trips drop the alias. `rg order_column packages --glob '!packages/contacting/**'` needed to size the blast radius — inside contacting, `HasContactMethods/SocialProfiles` correctly use `sort_order`, so the alias appears unused internally (speculative compat). | Why It Matters: Dual API with one broken direction; future devs sort/filter on the wrong key. | Recommended Fix: DELETE the alias (both `setAttribute`/`getAttribute` overrides + `order_column` from `$fillable`) and `rg order_column` repo-wide to migrate any external callers to `sort_order` in the same pass. If an external package genuinely needs `order_column` (e.g. a shared sortable contract), do the reverse: rename the COLUMN via migration — but this audit finds no such need, so delete. | Breaking Change: YES (any caller passing `order_column` now mass-assigns nothing — they must use `sort_order`; greppable, mechanical) | Affected Packages: `contacting`, `filament-contacting`, any `order_column` callers found by grep | Required Dependent Changes: Update flagged callers; update `docs/04-usage.md` ordering section. | Migration Required: NO
- Severity: Medium | Location: `packages/contacting/src/Actions/CreateContactSnapshotAction.php::persistSnapshot()` (lines 122–133: `if (!config(contact_snapshots)) return $snapshot;` unsaved), `fromBundle()` (N+M sequential saves, no transaction), `makeSnapshot()` (copies `owner` from source, never validates `snapshotable` owner) | Problem: (a) Feature-flag OFF returns a non-persisted model typed as `ContactSnapshot` — callers cannot distinguish saved vs phantom (no `wasRecentlyCreated`, `exists=false` silently). (b) `fromBundle` can half-persist (3 of 5 saved, then exception) with no rollback. (c) Snapshot `owner` inherited from SOURCE contact row, but `snapshotable` (e.g. an Order) may belong to a different owner — guard checks only `snapshotable` existence via `ContactingModelReferenceGuard` on the snapshot model's own `saving` (which resolves but doesn't compare owners). | Why It Matters: Phantom snapshots treated as history; partial bundles corrupt audit trails; cross-owner snapshot linkage. | Recommended Fix: (a) Throw `ContactSnapshotsDisabledException` (or return `null` with nullable return type — pick throw, explicit) instead of phantom return; (b) wrap `fromBundle` in `DB::transaction`; (c) in `makeSnapshot`, resolve `snapshotable` owner via guard and throw on source-owner ≠ snapshotable-owner mismatch unless explicit global context. | Breaking Change: YES (flag-off path now throws instead of phantom-return — intended; `fromBundle` atomicity only tightens) | Affected Packages: `contacting`, `orders`, `checkout` (snapshot-at-order-time callers) | Required Dependent Changes: Callers gate on the feature flag BEFORE calling (or catch); order code wraps bundle in owner context already — verify. | Migration Required: NO

## Code Quality Findings
- Severity: Low | Location: `packages/contacting/src/Concerns/HasContactMethods.php::addContactMethod()` (lines 136–153: `if (function_exists('app'))` fallback constructing `CreateContactMethodAction` manually) vs `HasSocialProfiles::addSocialProfile()` (direct `app(...)` no fallback) | Problem: Asymmetric container handling: `HasContactMethods` supports non-Laravel usage via manual construction while `HasSocialProfiles` hard-requires the container. The fallback constructs `NormalizeContactMethodAction(new NormalizesEmailAddress, ...)` bypassing container singletons/config (e.g. `propaganistas/laravel-phone` region config) — divergent normalization inside vs outside Laravel. | Why It Matters: Tests/seeds outside app context normalize differently. | Recommended Fix: Delete the `function_exists('app')` branch (package requires Laravel — `composer.json` has no non-Laravel support); always resolve via container. One-line deletion + matching docblock. | Breaking Change: NO (non-Laravel usage was never supported) | Affected Packages: `contacting` | Required Dependent Changes: None. | Migration Required: NO
- Severity: Low | Location: `packages/contacting/src/Actions/BuildContactLinksAction.php` (zero non-doc consumers — verified: only `contacting/docs` + CONTEXT reference it), `src/Data/ContactLinksData.php` | Problem: `BuildContactLinksAction` appears unused in code (docs-only references). Either dead or host-app-only surface. `rg BuildContactLinksAction packages --glob '!packages/contacting/**'` needed to confirm; if truly zero, it's speculative API. | Why It Matters: Unused public API rots (signature drift vs model fields). | Recommended Fix: Confirm with repo-wide grep; if zero code hits, either add ONE internal caller (e.g. Filament infolist `profileUrl` composition) or delete action + data class. Do not keep docs-only actions. | Breaking Change: YES if deleted while a host uses it (hosts are out of repo — announce in changelog) | Affected Packages: `contacting`, possibly host apps | Required Dependent Changes: None in-repo. | Migration Required: NO
- Severity: Low | Location: `packages/contacting/src/Concerns/HasContactMethods.php::resolveEmail/resolvePhone/resolveContact` + `HasSocialProfiles` resolvers | Problem: Resolvers load full models to extract one string and ignore `valid_from/valid_until` windows (compare `addressing.HasAddresses::primaryAddress`, which filters validity on both the in-memory and query paths). `ContactMethod::scopePublic` exists but the resolvers never filter it — a private primary email resolves identically to a public one. | Why It Matters: Expired/private contacts served as current; validity windows are write-only metadata. | Recommended Fix: Filter `valid_from/valid_until` in `primaryContactMethod()`/`resolveContact()` (mirror `addressing`'s `validAddressQuery` shape); add an explicit `$publicOnly` flag to the resolvers (default false, no behavior change) so directory features can opt into public-only reads. | Breaking Change: NO | Affected Packages: `contacting`, `customers`, `events`, `affiliates` | Required Dependent Changes: None (opt-in flag). | Migration Required: NO

## Laravel-Specific Findings
- Severity: Low | Location: `packages/contacting/composer.json` (`php ^8.4`, `propaganistas/laravel-phone ^6.0`, `spatie/laravel-data ^4.23`, `commerce-support self.version`) | Problem: PHP 8.4 baseline COMPLIANT (strict types, enums, `#[Override]`, constructor promotion, match). `laravel-phone` pinned correctly for phone normalization (do NOT reimplement libphonenumber logic — the package correctly delegates). | Why It Matters: Confirmation. | Recommended Fix: None. | Breaking Change: NO | Affected Packages: — | Required Dependent Changes: None. | Migration Required: NO
- Severity: Low | Location: `packages/contacting/database/migrations/*.php` | Problem: Constraint posture COMPLIANT (zero `constrained()`/`cascadeOnDelete()` — verified; `nullableUuidMorphs` + `uuidMorphs` without constraints; PKs `uuid('id')->primary()`; JSON via `commerce_json_column_type('contacting','json')` with `json_column_type` config present — key and helper agree). App cascades via `HasContactMethods/HasSocialProfiles::deleting` (`$model->contactMethods()->delete()`) — correct pattern but mass-delete bypasses per-row `saved` demote hooks (fine — rows are gone) and bypasses owner checks (delete cascades from an authorized parent delete — acceptable, document). Deviation: `timestamps()` (naive) vs `timestampsTz()` elsewhere — documented as NO-migration. | Why It Matters: Confirmation + one documented exception. | Recommended Fix: None now. | Breaking Change: NO | Affected Packages: — | Required Dependent Changes: None. | Migration Required: NO
- Severity: Medium | Location: `packages/contacting/config/contacting.php` | Problem: Config discipline: GOOD defaults (`defaults.country_code='MY'` env-overridable, `public_by_default=true`, `verified_by_default=false`, `features.owner` enabled/include_global/auto_assign all env-wired — exemplary). Gaps: (a) `features.contact_snapshots=true` hardcodes no env override (compare owner flags) — cannot disable per-env without code change (though disabling now throws per this audit's fix — wire `CONTACTING_SNAPSHOTS_ENABLED`); (b) `contact_methods.types` (8) vs `ContactMethodType` enum vs `social_profiles.platforms` (45) — three taxonomies, one file, no single validation entrypoint; (c) `table_prefix` + per-table env wiring present and correct — keep as template for `persons`/`organizations`. | Why It Matters: Flag without env = undeployable toggle. | Recommended Fix: `contact_snapshots => env('CONTACTING_SNAPSHOTS_ENABLED', true)`; document strict-flag rollout path. | Breaking Change: NO | Affected Packages: `contacting` | Required Dependent Changes: None. | Migration Required: NO
- Severity: Low | Location: `packages/contacting/src/ContactingServiceProvider.php` (config + migrations only, no bindings) | Problem: Lean provider — CORRECT (actions resolved via container auto-wiring; normalizer contracts bound implicitly by type-hint where needed). No `OwnerResolver` binding needed (consumer of `commerce-support`, not provider). | Why It Matters: Confirmation. | Recommended Fix: None. | Breaking Change: NO | Affected Packages: — | Required Dependent Changes: None. | Migration Required: NO
- Severity: Low | Location: `packages/contacting/src/` (no `Jobs/`, `Middleware/`, `Policies/`, `Observers/`, `Facades/`, `Http/`, `Commands/`) | Problem: No jobs/middleware/policies/observers/facades/commands — verified via `ls packages/contacting/src`. Correct for a synchronous polymorphic-attach layer (writes are request-scoped model hooks + actions; authorization delegates to `ContactingModelReferenceGuard` + the owning model's policy, not per-row policies; no async work to queue). | Why It Matters: Confirmation; prevents speculative scaffolding. | Recommended Fix: None. Do NOT add policies/commands unless a standalone moderation flow needs them. | Breaking Change: NO | Affected Packages: — | Required Dependent Changes: None. | Migration Required: NO
- Severity: Low | Location: `packages/contacting/` (no money columns — verified: `rg currency|Money|minor packages/contacting/src` returns zero hits) | Problem: None — money handling is correctly ABSENT (contacts carry no amounts; `defaults.country_code='MY'` is a phone-normalization default, not a currency). | Why It Matters: Confirmation. | Recommended Fix: None. | Breaking Change: NO | Affected Packages: — | Required Dependent Changes: None. | Migration Required: NO
- Severity: Low | Location: `packages/contacting/database/migrations/*.php` (no `state` column on any of the 3 tables; lifecycle is `valid_from/valid_until` windows + `verified_at` + `is_verified`) | Problem: `state` reservation COMPLIANT — geographic `state` correctly lives in `addressing`, never in contacting. | Why It Matters: Confirmation; blocks a naive "add state" refactor. | Recommended Fix: None. | Breaking Change: NO | Affected Packages: — | Required Dependent Changes: None. | Migration Required: NO
- Severity: Low | Location: `packages/contacting/src/` vs `packages/filament-contacting/src/` (dependency direction) | Problem: Direction COMPLIANT — verified: `rg Filament packages/contacting/src` returns zero hits (core never imports the adapter); `filament-contacting/composer.json` hard-requires `aiarmada/contacting: self.version` (correct). No Octane-unsafe static mutable state (verified: no mutable `static $` state; `SocialProfileConfig` reads config per-call). | Why It Matters: Confirmation; adapter removal never breaks core boot. | Recommended Fix: None. | Breaking Change: NO | Affected Packages: — | Required Dependent Changes: None. | Migration Required: NO

## Filament Adapter Findings
- Severity: Low | Location: `packages/filament-contacting/config/filament-contacting.php`, `ContactMethodResource.php:25,35`, `SocialProfileResource.php:25,35`, `ContactSnapshotResource.php:23,33`, `Support/ContactingFilamentConfig.php` | Problem: Navigation + adapter posture EXEMPLARY: nested `navigation.group='Contacting'` + `sort=70` + `icons.*`, every resource implements `getNavigationGroup()` + `getNavigationSort()` + `getNavigationIcon()` from config, `getEloquentQuery()` applies `OwnerUiScope::apply(..., includeGlobal:false)` on ALL THREE resources (matches core `include_global=false` default — consistent, verified), `GuardsContactingUi` gates create/edit pages by `read_only` config, relation-managers-first defaults (`standalone_resources=false`, all three `resources.*.enabled=false`) correctly avoid duplicate admin surfaces. Thin-adapter PASS (no domain logic; schemas/tables delegate to core normalization). | Why It Matters: Confirmation — use as the template for the other three filament packages. | Recommended Fix: None. | Breaking Change: NO | Affected Packages: — | Required Dependent Changes: None. | Migration Required: NO
- Severity: Medium | Location: `packages/filament-contacting/src/RelationManagers/ContactMethodsRelationManager.php`, `SocialProfilesRelationManager.php` (owner handling on attach), `src/Exports/*` | Problem: Verify (audit flags as unverified, not failed): relation-manager create/attach must set owner from the parent (`OwnerContext::withOwner` or `assignOwner`) AND revalidate submitted `contactable/snapshotable` IDs via `OwnerWriteGuard` (core guard exists — confirm the adapter calls it rather than trusting hidden form fields). Imports (`ContactMethodImporter`, `SocialProfileImporter`) now resolve owner per-row via `ContactingModelReferenceGuard` in `beforeValidate()` (implemented); exports must still scope rows via `OwnerUiScope` (confirm). | Why It Matters: Bulk import is the highest-volume forgery surface. | Recommended Fix: Add `OwnerUiScope` in exporters; Pest test with cross-owner CSV row (importer guard already in `beforeValidate`). | Breaking Change: NO | Affected Packages: `filament-contacting`, `contacting` | Required Dependent Changes: None. | Migration Required: NO
- Severity: Low | Location: `packages/filament-contacting/config/filament-contacting.php` (`tables.show_owner_columns=false`, `show_verification_columns=true`, `show_visibility_columns=true`) | Problem: `show_owner_columns=false` default hides owner context in admin while resources are owner-scoped — acceptable for single-tenant admin, but multi-tenant support debugging needs the toggle documented. No defect. | Why It Matters: Minor observability. | Recommended Fix: Document the toggle in `docs/04-usage.md`; no code change. | Breaking Change: NO | Affected Packages: — | Required Dependent Changes: None. | Migration Required: NO

## Database Findings
- Severity: Low | Location: `packages/contacting/database/migrations/2000_01_01_000001_create_contact_methods_table.php` (`type/purpose`, `value/normalized/display` texts, `country_code char(2)`, flags + `sort_order`, `metadata` json, indexes on type+purpose/flags/country) | Problem: Shape is GOOD: `purpose default 'general'`, `is_public default true` matches config, `sort_order` ordering, `valid_from/valid_until` validity windows (unused by `HasContactMethods::primaryContactMethod` — validity unfiltered on read; compare addressing which filters validity — minor inconsistency, document or filter). `state` column correctly ABSENT (no geographic meaning here — compliant with reservation). No lifecycle `status` column — validity window + verified flags cover it (acceptable, document). | Why It Matters: Mostly confirmation; validity-read gap is minor. | Recommended Fix: Filter `valid_from/valid_until` in `primaryContactMethod()`/`resolveContact()` (mirror `addressing.HasAddresses::validAddressQuery`), or document windows as write-only metadata. | Breaking Change: NO | Affected Packages: `contacting` | Required Dependent Changes: None. | Migration Required: NO
- Severity: Low | Location: `contact_social_profiles` table name vs config `social_profiles` | Problem: Migration FILENAME says `create_contact_social_profiles_table` while `SocialProfile::getTable()` reads `contacting.database.tables.social_profiles` (default `social_profiles`). If the migration's internal `$tableName` uses config (it does — `config('contacting.database.tables.social_profiles','social_profiles')`), the filename is merely misleading, not wrong. Confirm migrated table name in a fresh install; rename FILE only (class name anonymous — safe) for clarity. | Why It Matters: Contributor confusion ("which table?"). | Recommended Fix: Rename migration file to `..._create_social_profiles_table.php` (no schema change; filename-only). | Breaking Change: NO | Affected Packages: `contacting` | Required Dependent Changes: None. | Migration Required: NO

## Model-Domain Findings
- Severity: Low | Location: `packages/contacting/src/Data/ContactMethodData.php` (`::email/phone/website` named constructors), `SocialProfileData`, `ContactSnapshotData`, `ContactLinksData` | Problem: Data-layer is the BEST in batch (named constructors encode channel defaults). Adopters use them correctly (`affiliates.CreateAffiliate`, `customers.CreateCustomer/UpdateCustomerProfile`, `events.RegistrationService`). Keep as the integration pattern other packages copy. | Why It Matters: Confirmation. | Recommended Fix: None. | Breaking Change: NO | Affected Packages: — | Required Dependent Changes: None. | Migration Required: NO

## Security Findings
- Severity: High | Location: `ContactingModelReferenceGuard::resolve()` + `ContactMethod::guardContactableOwner` / `SocialProfile::guardSocialableOwner` / `ContactSnapshot::guardSnapshotableOwner` (all `saving` hooks) | Problem: Guards run on `saving` but NOT on `deleting`: `HasContactMethods::bootHasContactMethods::deleting → $model->contactMethods()->delete()` mass-deletes without per-row owner verification (relies on parent-delete authorization — correct IF parent delete was authorized, but relation-manager "delete all" paths may call the relation directly). `syncSiblingPrimaryFlags` demote queries (both models) run without owner scope (above) — a forged `contactable_id` on create demotes another tenant's primaries BEFORE the guard throws (guard runs in same `saving` event — order: `normalizeForSave → guardAllowedType → guardContactableOwner`; demote runs on `saved`, i.e. AFTER guard passes — safe ordering, but only because guard precedes save; document this ordering dependency). | Why It Matters: Ordering-dependent safety breaks if hooks reorder. | Recommended Fix: Assert guard-before-demote ordering with a comment + Pest test (forged contactable rejected, siblings untouched); add owner scope to demote queries (shared-helper fix above); verify relation-manager bulk deletes authorize the parent first. | Breaking Change: NO | Affected Packages: `contacting`, `filament-contacting`, all consumers | Required Dependent Changes: None. | Migration Required: NO
- Severity: Medium | Location: `is_public` default TRUE (`config defaults.public_by_default=true`, migration `default(true)`, `applyDefaultFlags` on creating) | Problem: New contact rows are PUBLIC by default — correct for social profiles/websites, WRONG default for emails/phones (PII). `scopePublic` + Filament `show_visibility_columns` expose them; `ContactSnapshot.is_public` copied from source (propagates the permissive default into immutable history). | Why It Matters: PII over-exposure by default; snapshots freeze the exposure. | Recommended Fix: Make default visibility channel-aware: `email/phone/mobile/whatsapp → is_public=false` unless explicitly set; `website/social → true`. Implement in `applyDefaultFlags()` (both models) gated by channel, keep config override. Backfill NOT required (existing rows keep flags; snapshots already frozen). | Breaking Change: YES (new email/phone rows default private — callers relying on public default must set explicitly; intended privacy tightening) | Affected Packages: `contacting`, `customers`, `events`, `affiliates`, `communications` | Required Dependent Changes: Public-directory features set `is_public=true` explicitly; update `docs/03-configuration.md`. | Migration Required: NO

## Performance Findings
- Severity: Low | Location: `ContactMethod::saved → syncSiblingPrimaryFlags` (transaction + locks per primary save), `HasContactMethods::resolveContact/resolveContacts` (per-type ordered queries), `CreateContactSnapshotAction::fromBundle` (N+M saves) | Problem: Primary-save does 1 lock-read + 1 mass-update per primary write (correct, infrequent). `resolveEmails/resolvePhones` load full models to extract one string (acceptable; add `select(value,normalized_value)` projection if hot). `fromBundle` N+M round-trips — wrap in transaction (above) and chunk if bundles exceed ~100. Indexes cover `(type,purpose)` + flags — good. | Why It Matters: No action at current scale; harden only the transaction. | Recommended Fix: Transaction for `fromBundle` (above) + optional column projection in resolvers. | Breaking Change: NO | Affected Packages: `contacting` | Required Dependent Changes: None. | Migration Required: NO

## Testing Findings
- Severity: High | Location: `packages/contacting/` + `packages/filament-contacting/` (no `tests/` in either — verified `ls`) | Problem: Zero coverage for the most-depended-upon package: normalization matrix (email/phone/URL/handle), primary-demote invariant (both channels + race), cross-tenant isolation (all 3 models), guard rejection (bad type/id, cross-owner parent), snapshot bundle atomicity + phantom-path, channel-aware visibility defaults, importer owner scoping, `ContactMethodData` constructors. Consumers (`customers`, `affiliates`, `events`) inherit the risk. | Why It Matters: A normalization or guard regression breaks every CRM/affiliate/event flow simultaneously. | Recommended Fix: Create `packages/contacting/tests/` (Pest `--parallel`): `OwnerScopingContractTests` for all 3 models, normalization dataset (MY + intl phones via `laravel-phone`), primary-demote test both channels, forged-parent rejection test, snapshot atomicity test, visibility-default test. Create `packages/filament-contacting/tests/`: relation-manager scoping, importer cross-owner row rejection, exporter scoping. | Breaking Change: NO | Affected Packages: `contacting`, `filament-contacting`, all consumers | Required Dependent Changes: None. | Migration Required: NO
- Severity: Low | Location: `packages/contacting/database/factories/*.php` (3 factories, zero test consumers) | Problem: Factories exist (`ContactMethodFactory`, `SocialProfileFactory`, `ContactSnapshotFactory`) but no suite exercises them — factory defaults for `is_public`/`is_primary`/`sort_order` vs the recommended channel-aware visibility change will drift unless updated together. | Why It Matters: New Pest suites built on stale factories test the wrong defaults. | Recommended Fix: In the same pass as the test suites, align factory defaults with the POST-fix invariants (channel-aware `is_public`, `sort_order` sequencing) and add one smoke test per factory. | Breaking Change: NO | Affected Packages: `contacting` | Required Dependent Changes: None. | Migration Required: NO

## Contact-Topology Cross-Check (agrees with customers.md, orders.md, events.md, persons.md)
- `contacting` rows (polymorphic `ContactMethod`/`SocialProfile` via `HasContactMethods`/`HasSocialProfiles`) are canonical for multi-value/history; native columns (`customers.Customer.email/phone`, `customers.Address.phone`, `events.EventOrganizer.email/phone`, `events.Venue` computed accessors, `affiliates.Affiliate.contact_email/website_url`) are documented write-through caches for the primary value, synced by the single `syncPrimaryColumns` helper. Same class names and direction as `customers.md` (which flags the hard `contacting` require + duplicated contact sync as its High findings), `orders.md` (silent on contact storage — verified 2026-09-07: zero CustomerResolver/contacting mentions — so no contradiction; checkout-adjacent identity flows resolve via `customers.CustomerResolver` per `customers.md`, whose underlying store is contacting rows), `events.md` (venue/organizer traits stay, delivery bridging goes through `communications`, not contacting), and `persons.md` (persons carry NO contact columns at all — contact data for a human lives in contacting rows attached to the `Customer`/organizer, never on `Person`).
- `contacting` owns NO address data (belongs to `addressing`) and NO message history (belongs to `communications`) — boundaries match all three sibling audits.

## Cross-Package Dependency Impact
| Dependent Package | Dependency on contacting | Impact | Required Change |
|---|---|---|---|
| `customers` (`Customer`, `Address` + traits + `ContactMethodData` in actions) | Heavy — traits baked in, resolver moves rows | Inherits primary-race + alias + visibility-default defects; column/row duality | Adopt shared demote helper (auto), channel-aware defaults (auto), `syncPrimaryColumns` helper (opt-in) |
| `affiliates` (`Affiliate` + traits + `ContactMethodData::email/phone/website` in `CreateAffiliate`) | Heavy — pending-accessor sync pattern | Same as customers; hook-ordering fragility | Same as customers; document hook order |
| `events` (`Venue`, `EventOrganizer`, `Event`, `EventSession`, `EventOccurrence`, `RegistrationService`) | Heavy — traits + `ContactMethodData` | Same as customers | Same as customers |
| `feedback` (`FeedbackResponse`, `FeedbackTestimonial`) | Traits (verify which) | Same (lower volume) | Same |
| `affiliate-network` (`AffiliateOffer`, `AffiliateSite`) | References (verify trait vs Data use) | Same | Same |
| `communications` | CONTEXT cross-ref (sending history vs contact points boundary) | Boundary correct — no code change unless link building adopted | Use `BuildContactLinksAction` or delete it (above) |
| `orders` / `checkout` | Indirect (via `customers`) | Email/phone resolution duality affects checkout identity | Resolve via rows-canonical rule once documented |
| `filament-contacting` | Requires `contacting` (correct) | Inherits all core fixes automatically | Importer/exporter owner hardening + tests |

## Recommended Refactor Plan
1. Correctness batch (one release): delete `order_column` alias (migrate callers found by grep); snapshot atomicity + throw-on-disabled + owner-mismatch check; channel-aware `is_public` defaults; `CONTACTING_SNAPSHOTS_ENABLED` env wiring; validity-window filtering on reads.
2. Canonical-rule docs (same release): column-vs-row write-through rule + per-consumer mapping table in `contacting/docs/04-usage.md` + consumer docs; `syncPrimaryColumns` helper; `BuildContactLinksAction` keep-or-delete verdict via grep.
3. Filament hardening (same release): exporter `OwnerUiScope`, relation-manager owner inheritance verification (importer guard already in `beforeValidate`).
4. Tests (same release): Pest suites both packages (`--parallel`) incl. `OwnerScopingContractTests`; no migration, Greg-verify `order_column` blast radius before deleting alias.

## Files Likely to Change
- `packages/contacting/src/Models/ContactMethod.php`, `SocialProfile.php` (alias deletion, visibility defaults, validity reads)
- `packages/contacting/src/Actions/CreateContactSnapshotAction.php` (transaction, throw, owner-mismatch)
- `packages/contacting/src/Concerns/HasContactMethods.php` (container simplification, `syncPrimaryColumns` helper, validity filtering)
- `packages/contacting/config/contacting.php` (`CONTACTING_SNAPSHOTS_ENABLED` env wiring)
- `packages/contacting/src/Actions/BuildContactLinksAction.php` (keep-or-delete verdict)
- `packages/contacting/docs/04-usage.md`, `03-configuration.md`, `CONTEXT.md`
- `packages/filament-contacting/src/Exports/*`, `src/RelationManagers/*` (owner hardening)
- `packages/customers/src/Models/Customer.php`, `Address.php`, `packages/affiliates/src/Models/Affiliate.php`, `packages/events/src/Models/*` (adopt sync helper — consumer follow-ups)
- New: `packages/contacting/tests/*`, `packages/filament-contacting/tests/*`

## Files / Code That Should Be Removed
- `packages/contacting/src/Models/ContactMethod.php:81-98` + `SocialProfile.php:83-100` — delete `setAttribute`/`getAttribute` `order_column` overrides; remove `'order_column'` from both `$fillable` arrays (after repo-wide `rg order_column` caller migration).
- `packages/contacting/src/Concerns/HasContactMethods.php:142-152` — delete the `if (function_exists('app'))` manual-construction fallback branch (always use container).
- `packages/contacting/src/Actions/BuildContactLinksAction.php` + `src/Data/ContactLinksData.php` — DELETE if repo-wide grep confirms zero code consumers (docs-only references do not count); otherwise wire one internal caller and keep.
- `packages/contacting/database/migrations/2000_01_01_000003_create_contact_social_profiles_table.php` — rename FILE to `..._create_social_profiles_table.php` for table-name clarity (no schema change).
- Do NOT remove: `ContactingModelReferenceGuard` (load-bearing on every write), `SocialProfileConfig` (45-platform URL logic), snapshot models/actions (fix, don't delete), `timestamps()` (leave naive — no migration).

## Final Recommended Architecture
- `contacting` remains the single canonical polymorphic contact layer: rows-canonical for multi-value/history, native columns as documented write-through caches synced by one shared helper; all three models stay owner-scoped with guard-before-demote ordering and channel-aware privacy defaults.
- One primary-demote implementation shared by both channels (transactional, locked, owner-scoped, NULL-safe, change-gated), no alias columns, explicit snapshot persistence (throw when disabled, atomic bundles, owner-consistent links).
- Taxonomy converges on pure enums for identity (`SocialPlatform`, `ContactMethodType`, `ContactPurpose`) with config reserved for display metadata; Filament stays relation-manager-first, resources off by default, imports/exports owner-hardened — the template the other three filament adapters copy.
