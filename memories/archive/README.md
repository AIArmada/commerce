# Archived Memories

This directory stores assistant notes that are no longer part of the active repo memory set.

- Treat archived memories as historical context only.
- Do not treat them as the current source of truth.
- When a point from an archived note becomes durable, it should be promoted into package docs or active root docs.